import React from "react";

const BackgroundImage = () => {
    return (
        <div style={{backgroundImage: `url(images/bg_image.jpeg)`}} className="flex h-screen bg-cover"></div>
    );
};

export default BackgroundImage;
